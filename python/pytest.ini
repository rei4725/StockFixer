[pytest]
markers =
    unit: Unit tests (no external dependencies, fast)
    integration: Integration tests (require DB, yfinance, etc.)
    slow: Slow tests that run actual CLI commands or require heavy resources
testpaths = tests
pythonpath = .
addopts = --timeout=30
