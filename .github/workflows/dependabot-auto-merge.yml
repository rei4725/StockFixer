name: Dependabot Auto-merge

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    runs-on: ubuntu-latest
    if: github.actor == 'dependabot[bot]'

    steps:
      - name: Fetch Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@v3
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"

      - name: Notify on security advisory patch update
        if: |
          steps.metadata.outputs.update-type == 'version-update:semver-patch' &&
          contains(github.event.pull_request.labels.*.name, 'security')
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body: ':warning: **セキュリティアドバイザリ由来の更新です。**\n\nこの PR は自動マージ対象ですが、セキュリティ修正を含むため変更内容を必ずご確認ください。'
            });

      # 注: 自動 approve は行わない。GITHUB_TOKEN による approve は 422 で失敗し、
      # かつ dependabot PR は require-review-approval.yml の対象外（承認不要）のため不要。
      - name: Enable auto-merge for patch updates
        if: steps.metadata.outputs.update-type == 'version-update:semver-patch'
        run: gh pr merge --auto --squash "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
