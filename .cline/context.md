## プロジェクト：CuteStock - 株式自動売買システム
- 目的：Pythonによる戦略ロジックとAI予測、C#による注文実行とUI表示を組み合わせたリアルタイム自動売買システムを構築し、利益を最大化する。
- 制約：
- 関連：
  - PROJECT_OVERVIEW.md を参照する

## タスク：株価データ特徴量拡張
- 目的：AIモデルの精度向上のため、テクニカル指標（RSI, MACD, EMA, ATR等）を追加し、全数値列のラグ特徴量を自動生成できるようにする。
- 前提：technical_analysis.py, data_saver.py の修正が必要。
- 進捗：2025-08-11に実装・完了。

## タスク：証券会社連携 機能を作成
- 目的：SBI証券との連携機能（ログイン、口座情報取得、株式売買）を実装する。
- 前提：SeleniumとWebDriver（Chrome Driver）のインストールとパス設定が必要。
- 進捗：ログイン機能の骨子、口座情報取得の仮実装、株式売買機能の骨子を実装済み。

---

## タスク：run_data_creation.py例外防止・yfinance仕様対応
- 目的：株価データ取得・保存スクリプトで例外が発生しないようにし、yfinanceのマルチインデックス仕様に対応する。
- 前提：yfinanceは複数銘柄やgroup_by="ticker"指定時にカラムがタプル型（MultiIndex）となるため、単一銘柄でもカラム名をフラット化する必要がある。
- 内容：get_stock_dataでカラム名を1段に変換し、後続処理（特徴量生成・保存）が正常動作するよう修正。
- 進捗：2025-08-11に実装・完了。

---

## タスク：出力ファイルパス・ファイル名設計改善
- 目的：データ出力の管理性・可読性向上のため、[market]_[symbol]サブディレクトリ＋用途明示ファイル名(features_YYYY_MM_DD_YYYY_MM_DD.csv)に統一する。
- 前提：data_saver.py の保存ロジック修正が必要。
- 内容：サブディレクトリ自動生成＋ファイル名改善。既存run_data_creation.py等の呼び出し方法は変更不要。
- 進捗：2025-08-11に実装・完了。

---

## タスク：バッチデータ生成自動化・既存csv削除
- 目的：バッチ処理によるデータ生成を自動化し、常に最新の特徴量データのみを保持することでデータの一貫性・保守性を高める。
- 前提：save_stock_data_with_featuresでstart_date, end_date自動決定・既存csv削除を一元管理。
- 内容：batch_run_data_creation.pyからrun_data_creation.pyを呼び出し、market/symbol単位で自動実行。データ保存前に同一ディレクトリ内の既存csvファイルを全削除。
- 進捗：2025-08-11に実装・完了。

### 更新履歴
- 2025-08-11 17:44：run_data_creation.py例外防止・yfinance仕様対応タスクの背景を追記
- 2025-08-11 16:58：株価データ特徴量拡張タスクの背景を追記
- 2025-08-11 18:32：バッチデータ生成自動化・既存csv削除タスクの背景を追記
